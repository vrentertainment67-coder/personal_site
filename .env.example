# Public Supabase config — safe to expose (anon publishable key).
# Safety comes from row-level security, not secrecy. Used by the RSVP forms,
# the door check-in, and the remix-download email capture.
PUBLIC_SUPABASE_URL=https://your-project.supabase.co
PUBLIC_SUPABASE_ANON_KEY=your-anon-publishable-key
